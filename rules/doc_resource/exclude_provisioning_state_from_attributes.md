Exclude `provisioning_state` from the Attributes Reference documentation. The provisioning state is an internal infrastructure detail and should not be exposed as a documented attribute.

## bad

```markdown
* `provisioning_state` - The provisioning state of the resource anchor.
```
